-- Return 10 percent row of a table
SELECT
	total.*
FROM 
(
    SELECT  
	@counter := @counter +1 AS counter,
	c.*
    FROM 
	(SELECT @counter:=0),
	app_customer c 
) total
WHERE counter <= (1/10 * @counter);
